{
  "name": "soyouwebsite",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm start",
    "start": "http-server -c-1 . -p 8080",
    "start:python": "python3 -m http.server 8080",
    "dev:app": "cd app && npm install && npm run dev",
    "build:app": "cd app && npm run build"
  },
  "description": "Static preview helpers for So You website",
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
